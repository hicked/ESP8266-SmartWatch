// #pragma once

// enum LogType {
//   PROD, ERROR, WARN, INFO, DEBUG
// };

// class Logger {
//   private:
//     char* id;
//     long start_millis;

//   public:
//     void log(char* message, LogType logType);
//     void time(char* id);
//     char* timeEnd(char* id);
// };
